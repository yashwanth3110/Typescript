const inputs=document.querySelectorAll('input');
console.log(inputs);
for(const input of inputs)
{
 
    console.log(input);
}
let arr:(string|number|boolean)[]=["yash","saju",21];
arr.push(true);
for(const temp of arr)
{
    console.log(temp);
}